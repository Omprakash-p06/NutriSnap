---
phase: 03-ux-personalization-p2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "frontend/src/services/planner/recipes.js",
  "frontend/src/services/planner/engine.js",
  "frontend/src/components/planner/MealPlanner.jsx"
]
autonomous: true
requirements: [REQ-P2-01]
must_haves:
  truths:
    - "System suggests meals that fit within remaining calorie budget"
    - "System prioritizes high-protein recipes when protein goal is not met"
  artifacts:
    - path: "frontend/src/services/planner/engine.js"
      provides: "Recommendation logic"
  key_links:
    - from: "src/components/planner/MealPlanner.jsx"
      to: "src/services/planner/engine.js"
      via: "import suggestMeals"
---

<objective>
Implement the rule-based Meal Planner engine. This provides value by telling users exactly what to eat next based on their real-time nutrition gaps.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-ux-personalization-p2/03-RESEARCH.md
@frontend/src/services/api.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Recipe Mock Database</name>
  <files>frontend/src/services/planner/recipes.js</files>
  <action>
    Create a constant array of 15-20 diverse recipes. 
    Each recipe must have: id, name, calories, protein, carbs, fat, tags (e.g., 'vegan', 'high-protein', 'quick').
    Ensure a mix of meal types (breakfast, lunch, dinner, snack).
  </action>
  <verify>
    <automated>node -e "import { recipes } from './frontend/src/services/planner/recipes.js'; if (recipes.length < 15) process.exit(1)"</automated>
  </verify>
  <done>Recipes array exported with correct schema</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Filtering Pipeline Engine</name>
  <files>frontend/src/services/planner/engine.js</files>
  <behavior>
    - suggestMeals(recipes, gaps): Should return top 3 recipes that fit within calorie gap.
    - If protein gap is > 20g, prioritize recipes with 'high-protein' tag or protein/kcal ratio > 0.1.
  </behavior>
  <action>
    Implement pure functions for the filtering pipeline:
    1. filterByCalories: Exclude recipes > remaining budget.
    2. scoreByMacros: Rank recipes based on how well they fill the largest macro gap.
    3. selectTop: Return top 3 results.
  </action>
  <verify>
    <automated>npm test -- --filter=planner-engine</automated>
  </verify>
  <done>Recommendation engine correctly suggests meals based on input gaps</done>
</task>

<task type="auto">
  <name>Task 3: Build MealPlanner UI Component</name>
  <files>frontend/src/components/planner/MealPlanner.jsx, frontend/src/components/planner/RecipeCard.jsx</files>
  <action>
    Create a functional component that accepts 'currentIntake' and 'targets' props.
    It should call 'suggestMeals' and render the results as cards.
    Each card shows name, calories, and key macro (e.g., '25g Protein').
    Add a 'Refresh Suggestions' button to randomize the top picks if more than 3 match.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('frontend/src/components/planner/MealPlanner.jsx', 'utf8'); if (!c.includes('suggestMeals') || !c.includes('RecipeCard')) process.exit(1)"</automated>
  </verify>
  <done>UI component renders suggestions and updates when inputs change</done>
</task>

</tasks>

<success_criteria>
- suggestMeals logic passes unit tests for calorie capping.
- UI displays exactly 3 cards on initial render.
- High-protein meals are suggested when protein intake is low.
</success_criteria>

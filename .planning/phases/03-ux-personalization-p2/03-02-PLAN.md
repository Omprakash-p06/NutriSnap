---
phase: 03-ux-personalization-p2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [
  "frontend/src/services/db.js",
  "frontend/src/services/aggregator.js",
  "frontend/src/hooks/useMealHistory.js"
]
autonomous: true
requirements: [REQ-P2-02]
must_haves:
  truths:
    - "Meal history persists after application restart without network"
    - "Daily totals are automatically recalculated when a meal is added"
  artifacts:
    - path: "frontend/src/services/db.js"
      provides: "IndexedDB schema"
    - path: "frontend/src/services/aggregator.js"
      provides: "Multi-day data reduction"
---

<objective>
Setup the offline-first data layer using Dexie.js and build the aggregation engine. This ensures the app remains functional and fast regardless of connectivity.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-ux-personalization-p2/03-RESEARCH.md
@frontend/src/hooks/useMealHistory.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Dexie.js Storage with userId</name>
  <files>frontend/src/services/db.js</files>
  <action>
    Install dexie if missing.
    Define the database schema:
    - meals: ++id, userId, timestamp, name, calories, protein, carbs, fat
    - dailyStats: [userId+date], userId, date, totalCalories, totalProtein, totalCarbs, totalFat
    Export a singleton 'db' instance.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('frontend/src/services/db.js', 'utf8'); if (!c.includes('userId') || !c.includes('dailyStats')) process.exit(1)"</automated>
  </verify>
  <done>Dexie database initialized with userId-aware schema</done>
</task>

<task type="auto">
  <name>Task 2: Implement Aggregator Service</name>
  <files>frontend/src/services/aggregator.js</files>
  <action>
    Use 'date-fns' to group meals by day.
    Implement 'calculateDailyTotal(meals)': Sum macros for a set of meals.
    Implement 'getWeeklySummary(db, userId)': Query last 7 days for the specific user from IndexedDB and return an array for Recharts.
    Integrate 'fitness-calc' to calculate TDEE if user profile is available.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('frontend/src/services/aggregator.js', 'utf8'); if (!c.includes('calculateDailyTotal') || !c.includes('getWeeklySummary')) process.exit(1)"</automated>
  </verify>
  <done>Aggregator can transform raw meal logs into chart-ready daily buckets</done>
</task>

<task type="auto">
  <name>Task 3: Refactor useMealHistory to Offline-First</name>
  <files>frontend/src/hooks/useMealHistory.js</files>
  <action>
    Update the hook to use Dexie live queries (useLiveQuery from dexie-react-hook).
    Replace existing state-only or API-only logic with Dexie operations.
    Ensure 'addMeal' includes the 'userId' from auth context and updates the 'meals' table.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('frontend/src/hooks/useMealHistory.js', 'utf8'); if (!c.includes('useLiveQuery')) process.exit(1)"</automated>
  </verify>
  <done>Meal history hook seamlessly manages IndexedDB sync with userId scoping</done>
</task>

</tasks>

<success_criteria>
- Meals added in the UI appear in IndexedDB immediately.
- Weekly summary returns exactly 7 data points.
- Data is scoped by userId, preventing cross-user data leakage.
</success_criteria>

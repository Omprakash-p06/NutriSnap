---
phase: 03-ux-personalization-p2
plan: 03
type: execute
wave: 2
depends_on: ["03-02"]
files_modified: [
  "frontend/src/components/dashboard/ProgressDashboard.jsx",
  "frontend/src/components/dashboard/MacroBreakdown.jsx",
  "frontend/src/pages/DashboardPage.jsx"
]
autonomous: true
requirements: [REQ-P2-02]
must_haves:
  truths:
    - "User can visualize their calorie trends over the last 7 days"
    - "Macro distribution is shown in a clear, interactive chart"
  artifacts:
    - path: "frontend/src/components/dashboard/ProgressDashboard.jsx"
      provides: "Visual analytics layer"
---

<objective>
Build the visualization layer of the Progress Dashboard using Recharts. This makes the data actionable and motivating for the user.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-ux-personalization-p2/03-RESEARCH.md
@frontend/src/services/aggregator.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ProgressDashboard with Recharts</name>
  <files>frontend/src/components/dashboard/ProgressDashboard.jsx</files>
  <action>
    Implement a responsive BarChart for calorie intake.
    - X-Axis: Days of the week.
    - Y-Axis: Calories.
    - Reference Line: Daily calorie target.
    Use 'isAnimationActive={false}' for better mobile performance as per research.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('frontend/src/components/dashboard/ProgressDashboard.jsx', 'utf8'); if (!c.includes('BarChart') || !c.includes('isAnimationActive={false}')) process.exit(1)"</automated>
  </verify>
  <done>Interactive BarChart renders correctly with mock/real data</done>
</task>

<task type="auto">
  <name>Task 2: Implement MacroBreakdown PieChart</name>
  <files>frontend/src/components/dashboard/MacroBreakdown.jsx</files>
  <action>
    Create a PieChart showing the percentage split of Carbs, Protein, and Fat for the current day.
    Use the green-centric color scheme from the design system.
    Add a legend with gram values.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('frontend/src/components/dashboard/MacroBreakdown.jsx', 'utf8'); if (!c.includes('PieChart')) process.exit(1)"</automated>
  </verify>
  <done>PieChart correctly visualizes macro distribution</done>
</task>

<task type="auto">
  <name>Task 3: Create Dashboard Page and Wiring</name>
  <files>frontend/src/pages/DashboardPage.jsx</files>
  <action>
    Create a dedicated Dashboard page.
    Wire it to 'aggregator.getWeeklySummary()' and the current user's profile goals.
    Ensure layout is mobile-first with stacked charts.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('frontend/src/pages/DashboardPage.jsx', 'utf8'); if (!c.includes('getWeeklySummary')) process.exit(1)"</automated>
  </verify>
  <done>Dashboard page integrates all visualizations with live data from Dexie</done>
</task>

</tasks>

<success_criteria>
- Dashboard shows 7 bars for the last 7 days.
- Reference line correctly reflects user's targets.
- Charts resize gracefully on mobile devices.
</success_criteria>

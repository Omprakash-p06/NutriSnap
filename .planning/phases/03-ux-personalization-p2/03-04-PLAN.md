---
phase: 03-ux-personalization-p2
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
files_modified: [
  "frontend/vite.config.js",
  "frontend/src/App.jsx",
  "frontend/src/components/common/UpdateToast.jsx"
]
autonomous: true
requirements: [NFR-03]
must_haves:
  truths:
    - "User can install the app on their home screen"
    - "User is notified when the app is running offline"
    - "User is prompted to update when a new version is available"
  artifacts:
    - path: "frontend/vite.config.js"
      provides: "PWA service worker configuration"
---

<objective>
Finalize the NutriSnap experience by implementing PWA capabilities and polishing the overall UX for a production-ready feel.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-ux-personalization-p2/03-RESEARCH.md
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Vite PWA Plugin</name>
  <files>frontend/vite.config.js</files>
  <action>
    Configure 'vite-plugin-pwa' with:
    - registerType: 'prompt'
    - manifest: Define name, short_name, theme_color, and icons.
    - workbox: Runtime caching for assets and Google Fonts.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('frontend/vite.config.js', 'utf8'); if (!c.includes('VitePWA')) process.exit(1)"</automated>
  </verify>
  <done>PWA plugin configured for offline usage and update prompts</done>
</task>

<task type="auto">
  <name>Task 2: Implement Offline & Update UX</name>
  <files>frontend/src/components/common/UpdateToast.jsx, frontend/src/App.jsx</files>
  <action>
    Build a toast component that appears when the service worker finds an update.
    Use 'useRegisterSW' hook from 'virtual:pwa-register/react'.
    Add a global 'Offline' banner when 'navigator.onLine' is false.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('frontend/src/components/common/UpdateToast.jsx', 'utf8'); if (!c.includes('useRegisterSW')) process.exit(1)"</automated>
  </verify>
  <done>App provides clear feedback for connectivity status and software updates</done>
</task>

<task type="auto">
  <name>Task 3: Final E2E Audit & Polish</name>
  <files>frontend/src/pages/Home.jsx, ROADMAP.md</files>
  <action>
    Perform a final sweep of the UI:
    - Ensure 'MealPlanner' and 'ProgressDashboard' are well-spaced on the Home page.
    - Check all 'Vanilla CSS' variables for consistency (green theme).
    - Update ROADMAP.md to mark Phase 3 as complete.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const road = fs.readFileSync('ROADMAP.md', 'utf8'); if (!road.includes('[x] Phase 3')) process.exit(1)"</automated>
  </verify>
  <done>NutriSnap is fully integrated, polished, and ready for deployment</done>
</task>

</tasks>

<success_criteria>
- App passes Lighthouse PWA audit (can be checked in browser).
- Toast appears when 'Update available' event is triggered.
- No 'dead ends' in the user flow; logging to planning to visualization is seamless.
</success_criteria>

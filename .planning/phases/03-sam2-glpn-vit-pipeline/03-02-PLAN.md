---
phase: 03-sam2-glpn-vit-pipeline
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/nutrisnap/utils/composite.py, scripts/preprocess_full.py]
autonomous: true
requirements: [PREP-04, MODL-01, MODL-02, ENG-02]
must_haves:
  truths:
    - "System can generate composite RGB+Mask+Depth images"
    - "Preprocessing script processes the 10-dish MVP subset using the 3-stage flow"
    - "Processed composite images are saved as .pt tensors"
  artifacts:
    - path: "src/nutrisnap/utils/composite.py"
      provides: "create_composite_image function"
    - path: "scripts/preprocess_full.py"
      provides: "Updated 3-stage preprocessing pipeline"
  key_links:
    - from: "scripts/preprocess_full.py"
      to: "src/nutrisnap/pipeline/segmenter.py"
      via: "Using FoodSegmenterSAM2"
    - from: "scripts/preprocess_full.py"
      to: "src/nutrisnap/pipeline/depth.py"
      via: "Using DepthEstimatorGLPN"
---

<objective>
Update the preprocessing pipeline to integrate SAM 2 and GLPN, and generate 5-channel composite images (RGB + Mask + Depth) for the MVP dishes.
This data preparation stage is critical for training the final ViT mass regressor.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@misc/strategy_final_2026-04-16.md
@src/nutrisnap/pipeline/segmenter.py
@src/nutrisnap/pipeline/depth.py
@scripts/preprocess_full.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Composite Image Utility</name>
  <files>src/nutrisnap/utils/composite.py</files>
  <action>
    Create a utility function `create_composite_image(rgb_tensor, mask_tensor, depth_tensor)` in `src/nutrisnap/utils/composite.py`.
    - `rgb_tensor`: (3, 224, 224)
    - `mask_tensor`: (1, 224, 224) binary
    - `depth_tensor`: (1, 224, 224) normalized
    - The function should concatenate these into a (5, 224, 224) tensor.
    - Optionally implement "Alpha Compositing" where RGB and Depth are multiplied by the Mask before concatenation to clear background noise.
  </action>
  <verify>
    <automated>python -c "import torch; from nutrisnap.utils.composite import create_composite_image; r=torch.randn(3,224,224); m=torch.ones(1,224,224); d=torch.randn(1,224,224); c=create_composite_image(r,m,d); print(c.shape)"</automated>
  </verify>
  <done>Composite generation utility works and returns a 5-channel tensor.</done>
</task>

<task type="auto">
  <name>Task 2: Update Preprocessing Script for 3-Stage Flow</name>
  <files>scripts/preprocess_full.py</files>
  <action>
    Modify `scripts/preprocess_full.py` to use the new 3-stage flow:
    1. RGB Preprocessing (existing logic but integrated with SAM 2).
    2. Depth Estimation via `DepthEstimatorGLPN` (instead of relying on raw depth_raw.png where missing).
    3. Food Segmentation via `FoodSegmenterSAM2`.
    4. Composite generation via `create_composite_image`.
    5. Save the final 5-channel tensor as `{dish_id}_composite.pt`.
    
    Ensure the script respects the 4GB VRAM budget by processing images sequentially if needed (using `torch.cuda.empty_cache()` if memory is tight).
  </action>
  <verify>
    <automated>python scripts/preprocess_full.py --dish-id dish_1550796634 --output-dir data/processed/mvp_test</automated>
  </verify>
  <done>Preprocessing script updated and verified on a single dish.</done>
</task>

<task type="auto">
  <name>Task 3: Preprocess 10-Dish MVP Subset</name>
  <files>data/processed/features/</files>
  <action>
    Run the updated `scripts/preprocess_full.py` for all dishes in the 10-dish MVP subset.
    - Use `configs/data/selected_dishes.json` to get the dish IDs (or the `--mvp-only` flag if integrated).
    - Monitor VRAM usage.
    - Verify that `data/processed/features/` contains `{dish_id}_composite.pt` for all target dishes.
  </action>
  <verify>
    <automated>ls data/processed/features/*_composite.pt | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>Full MVP subset preprocessed with the 3-stage pipeline.</done>
</task>

</tasks>

<verification>
Check `data/processed/features/` for the new composite tensor files.
Load a sample composite tensor and verify it has 5 channels.
</verification>

<success_criteria>
- All 10 MVP dishes have composite tensors generated.
- Composite tensors contain RGB, SAM 2 Mask, and GLPN Depth.
- VRAM management successful throughout the run.
</success_criteria>

<output>
After completion, create `.planning/phases/03-sam2-glpn-vit-pipeline/03-02-SUMMARY.md`
</output>

---
phase: 03-sam2-glpn-vit-pipeline
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [src/nutrisnap/models/vit_regressor.py, src/nutrisnap/training/train_vit.py]
autonomous: true
requirements: [MODL-01, MODL-04, MODL-05, EVAL-03]
must_haves:
  truths:
    - "ViT-based mass regressor can be trained on composite images"
    - "The model predicts total food mass in grams"
    - "Training achieves improving MSE loss on the MVP subset"
  artifacts:
    - path: "src/nutrisnap/models/vit_regressor.py"
      provides: "ViTRegressor class"
    - path: "src/nutrisnap/training/train_vit.py"
      provides: "Training script for ViT regressor"
  key_links:
    - from: "src/nutrisnap/models/vit_regressor.py"
      to: "transformers.VitModel"
      via: "ViT backbone loading"
---

<objective>
Implement and train the final stage of the pipeline: the Vision Transformer (ViT) mass regressor.
This model takes the 5-channel composite images and predicts the total food mass in grams.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@misc/strategy_final_2026-04-16.md
@data/processed/features/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ViT Regressor Architecture</name>
  <files>src/nutrisnap/models/vit_regressor.py</files>
  <action>
    Implement `ViTRegressor` in `src/nutrisnap/models/vit_regressor.py`.
    - Use `transformers.ViTModel` as the backbone (e.g., "google/vit-base-patch16-224").
    - Modify the input patch embedding to accept 5 channels (instead of 3).
    - Add a regression head (Linear layers) to predict a single scalar: mass in grams.
    - Implement the `forward(pixel_values)` method.
  </action>
  <verify>
    <automated>python -c "import torch; from nutrisnap.models.vit_regressor import ViTRegressor; m = ViTRegressor(); x = torch.randn(1, 5, 224, 224); print(m(x).shape)"</automated>
  </verify>
  <done>ViTRegressor implemented and handles 5-channel inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ViT Training Script</name>
  <files>src/nutrisnap/training/train_vit.py</files>
  <action>
    Create `src/nutrisnap/training/train_vit.py` for training the ViT regressor on the 10-dish MVP subset.
    - Load composite tensors from `data/processed/features/`.
    - Use labels (mass in grams) from `data/interim/dishes.csv`.
    - Use AdamW optimizer with a learning rate of 1e-4.
    - Implement MSE loss for regression.
    - Support 5-fold stratified CV or a simple train/val split for the MVP.
    - Ensure training runs within 4GB VRAM (use small batch size + grad accumulation).
  </action>
  <verify>
    <automated>python src/nutrisnap/training/train_vit.py --epochs 1 --batch-size 1 --limit-batches 2</automated>
  </verify>
  <done>Training script implemented and verified with a smoke run.</done>
</task>

<task type="auto">
  <name>Task 3: Execute Training on MVP Subset</name>
  <files>models/checkpoints/vit_mass_regressor.pth</files>
  <action>
    Run the training for 30 epochs on the 10-dish MVP subset.
    - Monitor MAE and R² metrics.
    - Save the best model checkpoint to `models/checkpoints/vit_mass_regressor.pth`.
    - Verify that MAE on the validation set is trending downwards.
  </action>
  <verify>
    <automated>python src/nutrisnap/training/train_vit.py --epochs 30 --batch-size 16</automated>
  </verify>
  <done>ViT regressor trained on MVP subset and achieving promising accuracy.</done>
</task>

</tasks>

<verification>
Check for the saved model checkpoint.
Review training logs for MAE targets (target ≤ 40 kcal, which correlates with mass MAE).
</verification>

<success_criteria>
- ViTRegressor architecture implemented.
- Training script functional.
- Best model checkpoint saved.
- Validation MAE target reached for the MVP subset.
</success_criteria>

<output>
After completion, create `.planning/phases/03-sam2-glpn-vit-pipeline/03-03-SUMMARY.md`
</output>

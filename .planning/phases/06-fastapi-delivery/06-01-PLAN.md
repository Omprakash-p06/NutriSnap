---
plan: "06-01"
phase: 6
wave: 1
completed: false
depends_on: []
files_modified:
  - src/nutrisnap/api/main.py
  - src/nutrisnap/api/models.py
  - src/nutrisnap/api/store.py
  - src/nutrisnap/api/worker.py
requirements_addressed: [API-01, API-02]
autonomous: true
---

# Plan 06-01: FastAPI Foundation & Async Jobs

Setup the boilerplate for the production API, including Pydantic models, SQLite task persistence, and the background job orchestration.

## Must Haves

- [ ] Directory `src/nutrisnap/api/` with `__init__.py`.
- [ ] `models.py`: `PredictionJob` and `PredictionResult` Pydantic schemas.
- [ ] `store.py`: `ResultStore` class using `aiosqlite` to track job status.
- [ ] `worker.py`: `JobWorker` class to handle async execution.
- [ ] `main.py`: FastAPI app with `/predict` and `/result/{id}` endpoints.

## Tasks

<task id="06-01-T1" title="Setup API structure and models">
  <wave>1</wave>
  <action>
**Create `src/nutrisnap/api/models.py`**:
Implement request/response schemas. Include `PredictionStatus` Enum (PENDING, PROCESSING, COMPLETED, FAILED).
  </action>
</task>

<task id="06-01-T2" title="Implement Result Store">
  <wave>1</wave>
  <action>
**Create `src/nutrisnap/api/store.py`**:
Implement async SQLite persistence for jobs. Table `jobs` with `id`, `status`, `result_json`, `created_at`.
  </action>
</task>

<task id="06-01-T3" title="Create Main API and Routing">
  <wave>1</wave>
  <action>
**Create `src/nutrisnap/api/main.py`**:
- `POST /predict` accepts image, saves to `data/uploads/`, returns Job ID.
- `GET /result/{job_id}` fetches from Store.
  </action>
</task>

## Verification

- [ ] `pytest tests/test_api.py` (Smoke test for endpoint existence and storage).
- [ ] Manual `curl` to verify job ingestion.

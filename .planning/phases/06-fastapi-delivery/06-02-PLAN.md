---
plan: "06-02"
phase: 6
wave: 2
completed: false
depends_on: ["06-01"]
files_modified:
  - src/nutrisnap/api/worker.py
  - src/nutrisnap/api/main.py
  - configs/api/config.yaml
requirements_addressed: [API-03, VERI-01]
autonomous: true
---

# Plan 06-02: Pipeline Integration & Performance

Integrate the modular pipeline (FoodSAM, Volume, Nutrition Model) into the background worker. Implement GPU locking and optimize for the 2s production budget.

## Must Haves

- [ ] `configs/api/config.yaml`: Configuration for model paths and thresholds.
- [ ] `worker.py`: Full integration of `FoodSegmenter`, `VolumeEstimator`, `NutritionPredictor`, and `NutritionValidator`.
- [ ] Latency measurements to ensure <2s performance on target hardware.

## Tasks

<task id="06-02-T1" title="Define API Configuration">
  <wave>2</wave>
  <action>
**Create `configs/api/config.yaml`**:
Define paths to model checkpoints and the default pipeline sequence.
  </action>
</task>

<task id="06-02-T2" title="Implement Pipeline Orchestrator in Worker">
  <wave>2</wave>
  <action>
**Update `src/nutrisnap/api/worker.py`**:
- Load all required models on initialization.
- Implement the sequence: Segmentation -> Volume -> Nutrition Regression -> Validation.
- Handle `asyncio.to_thread` for CPU/GPU heavy sync calls.
  </action>
</task>

<task id="06-02-T3" title="Optimization & Latency Check">
  <wave>2</wave>
  <action>
**Measure Latency**:
Insert timing logs between each step. Optimize any bottleneck identified.
  </action>
</task>

## Verification

- [ ] Full end-to-end integration test with real models.
- [ ] Verify that `VERI-01` rule-based validation is active.

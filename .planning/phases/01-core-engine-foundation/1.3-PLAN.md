# Plan 1.3: Auth & Profile System

**Goal**: Implement JWT-based user authentication and personalized calorie targeting.

## User Review Required
- No major design decisions required.

## Proposed Changes

### [NEW] `server/api/auth.py`
- Endpoints: `POST /auth/signup`, `POST /auth/login`.
- JWT token generation and validation.

### [NEW] `server/utils/nutrition.py`
- `calculate_bmr()`: Mifflin-St Jeor implementation.
- `calculate_tdee()`: Activity multiplier logic.

### [NEW] `server/models/user.py`
- Pydantic and MongoDB schemas for User profiles.

## Verification Plan
### Automated Tests
- Unit tests for `calculate_bmr` with edge case ages and sexes.
- Integration tests for Signup -> Login -> Token Validation flow.

### Manual Verification
- Register a new user via the API and verify the profile data in MongoDB.

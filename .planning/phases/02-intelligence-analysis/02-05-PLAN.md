---
phase: 02-intelligence-analysis
plan: 05
type: execute
wave: 3
depends_on: [02-02, 02-04]
files_modified: [frontend/src/hooks/usePrediction.js, frontend/src/components/MultiFoodDisplay.jsx, frontend/src/components/ChatBot.jsx]
autonomous: true
requirements: [INTELL-05]
must_haves:
  truths:
    - "Frontend displays itemized list of detected foods"
    - "User can chat with NutriBot about their meal"
  artifacts:
    - path: "frontend/src/components/MultiFoodDisplay.jsx"
      provides: "Itemized result UI"
  key_links:
    - from: "usePrediction hook"
      to: "/predict/status/{task_id}"
      via: "polling loop"
---

<objective>
Build the frontend components to handle asynchronous multi-food results and provide an interactive AI nutritionist chat interface.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@frontend/src/App.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Async Prediction Hook and UI</name>
  <files>frontend/src/hooks/usePrediction.js, frontend/src/components/MultiFoodDisplay.jsx</files>
  <action>
    - Create `usePrediction` custom hook that handles:
      1. Image upload.
      2. Polling `/predict/status/{task_id}` until completion or failure.
    - Create `MultiFoodDisplay` component to render the itemized list of foods, ingredients, and reasoning.
  </action>
  <verify>
    <automated>npm test frontend/src/components/__tests__/MultiFoodDisplay.test.js</automated>
  </verify>
  <done>Frontend can successfully trigger and display background analysis results.</done>
</task>

<task type="auto">
  <name>Task 2: Build Chat Interface Component</name>
  <files>frontend/src/components/ChatBot.jsx</files>
  <action>
    - Create a floating `ChatBot` component that connects to the `/chat/ws` WebSocket.
    - Implement message history state and auto-scrolling to the latest message.
    - Add a "Snap & Ask" feature where clicking a detected food item can pre-populate a question in the chat.
  </action>
  <verify>
    <automated>npm test frontend/src/components/__tests__/ChatBot.test.js</automated>
  </verify>
  <done>Interactive AI chat is fully functional on the frontend.</done>
</task>

</tasks>

<verification>
Manually verify the full flow: snap a photo, wait for analysis, view itemized results, and ask the chatbot a question about the results.
</verification>

<success_criteria>
- Frontend polls correctly without flooding the network.
- Results are displayed item-by-item with clear formatting.
- Chat interface is responsive and maintains conversation state.
</success_criteria>

<output>
After completion, create `.planning/phases/02-intelligence-analysis/02-05-SUMMARY.md`
</output>

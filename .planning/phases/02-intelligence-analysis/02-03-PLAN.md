---
phase: 02-intelligence-analysis
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [backend/app/utils/mapping.py, backend/data/ingredients.csv, backend/nutrisnap/pipeline/orchestrator.py]
autonomous: true
requirements: [INTELL-02]
must_haves:
  truths:
    - "Food labels are mapped to specific ingredient lists"
    - "Fuzzy matching resolves 'pizza' vs 'Pizza'"
  artifacts:
    - path: "backend/app/utils/mapping.py"
      provides: "CSV-based ingredient lookup"
  key_links:
    - from: "Orchestrator"
      to: "MappingService"
      via: "lookup(label)"
---

<objective>
Develop an ingredient mapping service that looks up detailed nutritional data and ingredient lists from a CSV database based on identified food labels.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-intelligence-analysis/02-RESEARCH.md
@backend/data/food_database.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MappingService and CSV Database</name>
  <files>backend/app/utils/mapping.py, backend/data/ingredients.csv</files>
  <action>
    - Create `ingredients.csv` with at least 50 common food items (label, ingredients, kcal_100g, protein, carbs, fat).
    - Implement `MappingService` class as a singleton that loads the CSV into memory.
    - Implement a lookup method with fuzzy matching (using `difflib` or similar) to handle minor spelling differences or variations in labels.
  </action>
  <verify>
    <automated>pytest backend/tests/test_mapping_service.py</automated>
  </verify>
  <done>Labels are successfully mapped to detailed ingredient and nutritional data.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate MappingService into Pipeline</name>
  <files>backend/nutrisnap/pipeline/orchestrator.py</files>
  <action>
    - Update the `SequentialOrchestrator` to call `MappingService.lookup(label)` for each detected item.
    - Enrich the `PredictedItem` object with the retrieved ingredients and standard macro ratios.
  </action>
  <verify>
    <automated>python backend/scratch/verify_enriched_results.py</automated>
  </verify>
  <done>Pipeline results now include ingredient breakdowns and standard macro estimates for detected foods.</done>
</task>

</tasks>

<verification>
Run a test image through the orchestrator and verify the output contains an `ingredients` field for each detected object.
</verification>

<success_criteria>
- Labels are accurately mapped to CSV entries.
- Fuzzy matching handles slight label variations.
- Pipeline output includes detailed ingredient strings.
</success_criteria>

<output>
After completion, create `.planning/phases/02-intelligence-analysis/02-03-SUMMARY.md`
</output>

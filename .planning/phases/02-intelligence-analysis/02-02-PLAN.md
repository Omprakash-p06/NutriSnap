---
phase: 02-intelligence-analysis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/utils/tasks.py, backend/app/routers/prediction.py, backend/app/main.py]
autonomous: true
requirements: [INTELL-04]
must_haves:
  truths:
    - "System returns 202 Accepted and task_id for inference requests"
    - "Status endpoint returns current task state and final results"
  artifacts:
    - path: "backend/app/utils/tasks.py"
      provides: "Background task tracking"
  key_links:
    - from: "POST /predict/validated"
      to: "BackgroundTasks"
      via: "FastAPI integration"
---

<objective>
Implement an asynchronous task management system to handle long-running food analysis without blocking the API or causing timeouts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-intelligence-analysis/02-RESEARCH.md
@backend/app/routers/prediction.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AsyncTaskManager</name>
  <files>backend/app/utils/tasks.py</files>
  <action>
    - Implement `AsyncTaskManager` using an in-memory dictionary or MongoDB to track task status (`pending`, `processing`, `completed`, `failed`).
    - Include methods for `update_status(task_id, status, result)` and `get_status(task_id)`.
  </action>
  <verify>
    <automated>pytest backend/tests/test_task_manager.py</automated>
  </verify>
  <done>Task manager can correctly track and retrieve the status of background jobs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Async Prediction Endpoints</name>
  <files>backend/app/routers/prediction.py, backend/app/main.py</files>
  <action>
    - Update `POST /predict/validated` to:
      1. Generate a `task_id`.
      2. Set status to `pending`.
      3. Trigger `BackgroundTasks` with the orchestrator.
      4. Return `202 Accepted` with the `task_id`.
    - Implement `GET /predict/status/{task_id}` to return current status and result.
  </action>
  <verify>
    <automated>curl -X POST /predict/validated -F "file=@test.jpg" && curl /predict/status/{id}</automated>
  </verify>
  <done>Analysis runs in the background, and the frontend can poll for results.</done>
</task>

</tasks>

<verification>
Perform a full upload and poll cycle using `curl` or a test script, ensuring the initial response is immediate and the final result contains the analysis.
</verification>

<success_criteria>
- Analysis does not block the API event loop.
- Task status is accurately reported.
- Results are accessible via the status endpoint once complete.
</success_criteria>

<output>
After completion, create `.planning/phases/02-intelligence-analysis/02-02-SUMMARY.md`
</output>

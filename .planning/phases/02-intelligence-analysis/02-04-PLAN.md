---
phase: 02-intelligence-analysis
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified: [backend/nutrisnap/pipeline/validator.py, backend/app/routers/chat.py, backend/app/routers/prediction.py]
autonomous: true
requirements: [INTELL-03]
must_haves:
  truths:
    - "Gemini validates pipeline output for plausibility"
    - "AI Chat references user's meal history"
  artifacts:
    - path: "backend/app/routers/chat.py"
      provides: "Context-aware AI nutritionist"
  key_links:
    - from: "NutritionValidator"
      to: "Gemini API"
      via: "validation prompt"
---

<objective>
Integrate Gemini AI for validating nutrition predictions and providing a context-aware nutrition assistant that uses user profile data and meal history.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-intelligence-analysis/02-RESEARCH.md
@backend/nutrisnap/pipeline/validator.py
@backend/app/routers/chat.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Gemini-based validation and reasoning</name>
  <files>backend/nutrisnap/pipeline/validator.py, backend/app/routers/prediction.py</files>
  <action>
    - Update `NutritionValidator` to optionally use Gemini for a "common sense" check of the detection results (e.g., "Is 500g of broccoli plausible for this image?").
    - Implement an `llm_reasoning` field in the pipeline output that uses Gemini to describe *why* certain nutritional values were estimated.
  </action>
  <verify>
    <automated>python backend/scratch/test_gemini_validation.py</automated>
  </verify>
  <done>Gemini provides qualitative validation and reasoning for nutrition estimates.</done>
</task>

<task type="auto">
  <name>Task 2: Contextualize AI Assistant</name>
  <files>backend/app/routers/chat.py</files>
  <action>
    - Update the Chat WebSocket to fetch the `current_user` profile and recent meal logs from the database.
    - Inject this context into the `_SYSTEM_PROMPT` to allow the assistant to provide personalized advice (e.g., "You've had a lot of protein today, maybe focus on fiber for dinner").
  </action>
  <verify>
    <automated>pytest backend/tests/test_contextual_chat.py</automated>
  </verify>
  <done>AI Assistant provides personalized advice based on user history and goals.</done>
</task>

</tasks>

<verification>
Connect to the chat WebSocket and ask "How is my progress today?". Verify it references actual meal log data from the current user session.
</verification>

<success_criteria>
- Gemini provides reasoning for each analyzed meal.
- Validation identifies and flags biologically implausible estimates.
- AI Assistant correctly identifies and references user meal history.
</success_criteria>

<output>
After completion, create `.planning/phases/02-intelligence-analysis/02-04-SUMMARY.md`
</output>

---
phase: 02-intelligence-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/nutrisnap/pipeline/multi_food.py, backend/nutrisnap/pipeline/orchestrator.py, backend/requirements.txt]
autonomous: true
requirements: [INTELL-01]
must_haves:
  truths:
    - "Pipeline executes YOLOv5 for initial object detection"
    - "Peak VRAM usage remains below 4GB during full inference"
  artifacts:
    - path: "backend/nutrisnap/pipeline/orchestrator.py"
      provides: "Sequential model lifecycle management"
  key_links:
    - from: "MultiFoodDetector"
      to: "torch.cuda.empty_cache()"
      via: "unload() method"
---

<objective>
Replace YOLOv8 with YOLOv5 and implement a sequential VRAM orchestration pattern to ensure the pipeline operates within 4GB VRAM limits.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-intelligence-analysis/02-RESEARCH.md
@backend/nutrisnap/pipeline/multi_food.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate YOLOv5 and update requirements</name>
  <files>backend/requirements.txt, backend/nutrisnap/pipeline/multi_food.py</files>
  <action>
    - Add `yolov5` to `backend/requirements.txt`.
    - Modify `backend/nutrisnap/pipeline/multi_food.py` to use `yolov5` (via `torch.hub.load('ultralytics/yolov5', 'yolov5s')` or similar stable method) instead of `ultralytics` YOLOv8.
    - Implement the `unload()` method to explicitly call `del self.model`, `gc.collect()`, and `torch.cuda.empty_cache()` as per research.
  </action>
  <verify>
    <automated>pytest backend/tests/test_yolov5_integration.py</automated>
  </verify>
  <done>YOLOv5 is integrated and the unload method successfully clears VRAM.</done>
</task>

<task type="auto">
  <name>Task 2: Create Sequential Pipeline Orchestrator</name>
  <files>backend/nutrisnap/pipeline/orchestrator.py</files>
  <action>
    - Create `SequentialOrchestrator` class that manages the lifecycle of models (Detector, Segmenter, Depth, Mass).
    - Implement a `run_pipeline` method that:
      1. Loads Detector -> Detects -> Unloads.
      2. Loads Segmenter -> Segments -> Unloads.
      3. Loads Depth -> Estimates -> Unloads.
      4. Loads Mass -> Estimates -> Unloads.
    - Ensure each step uses the `unload()` pattern to keep VRAM usage strictly sequential.
  </action>
  <verify>
    <automated>python backend/scratch/check_vram_usage.py</automated>
  </verify>
  <done>Orchestrator executes the full pipeline while maintaining VRAM usage below 4GB peak.</done>
</task>

</tasks>

<verification>
Run the `check_vram_usage.py` script to ensure peak VRAM does not exceed 4GB during a full multi-food inference run.
</verification>

<success_criteria>
- YOLOv5 detects multiple food items.
- VRAM is cleared between model loads.
- Pipeline completes without Out-Of-Memory (OOM) errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-intelligence-analysis/02-01-SUMMARY.md`
</output>

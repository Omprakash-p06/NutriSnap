---
plan: "05-02"
phase: 5
wave: 1
completed: false
depends_on: []
files_modified:
  - src/nutrisnap/pipeline/validator.py
  - configs/pipeline/validator.yaml
requirements_addressed: [VERI-01]
autonomous: true
---

# Plan 05-02: Rule-Based Nutrition Validator

Build a rule engine to catch physically or biologically implausible nutrition estimates before they reach the user.

## Objective

Prevent "hallucinated" regression results (e.g., 2000 calories for a small apple) by using geometric and biochemical constraints.

## Must Haves

- [ ] `NutritionValidator` class in `src.nutrisnap.pipeline.validator`.
- [ ] Atwater Factor consistency check (4-4-9).
- [ ] Energy Density gate (kcal / cm³ logic).
- [ ] Height-to-Area ratio check for geometric sanity.
- [ ] Configurable thresholds for flagging vs. blocking.

## Tasks

<task id="05-01-T1" title="Define validator configuration">
  <wave>1</wave>
  <action>
**Create `configs/pipeline/validator.yaml`**:
```yaml
validator:
  atwater_tolerance: 0.20  # 20% mismatch allowed
  density_range: [0.1, 9.0] # kcal/cm3
  min_confidence: 0.5
  flag_only: false        # block if failed
```
  </action>
</task>

<task id="05-01-T2" title="Implement NutritionValidator logic">
  <wave>1</wave>
  <action>
**Create `src/nutrisnap/pipeline/validator.py`**:
Implement `validate(predictions, volume, area)` returning `(is_plausible, reason)`.
  </action>
</task>

## Verification

- [ ] Unit tests in `tests/test_validator.py` confirm that absurd nutrition/volume combinations are flagged.

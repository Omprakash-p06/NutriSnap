---
plan: "05-03"
phase: 5
wave: 2
completed: false
depends_on: ["05-02"]
files_modified:
  - src/nutrisnap/pipeline/fallback.py
requirements_addressed: [VERI-02]
autonomous: true
---

# Plan 05-03: LLM Fallback (Gemini Flash)

Integrate an LLM-based verification path to provide a fallback opinion when the regression model is uncertain or the validator flags a result.

## Objective

Increase system reliability by using multimodal AI (Gemini Flash) to audit anomalous regression outputs.

## Must Haves

- [ ] `LLMVerifier` class in `src.nutrisnap.pipeline.fallback`.
- [ ] Integration with `google-generativeai` (Gemini 1.5 Flash).
- [ ] Prompt template for visual nutrition estimation.
- [ ] Logic to compare Model vs. LLM results and flag discrepancies.

## Tasks

<task id="05-03-T1" title="Implement LLM verifier layer">
  <wave>1</wave>
  <action>
**Create `src/nutrisnap/pipeline/fallback.py`**:
Implement a class that:
1. Accepts an RGB image.
2. Calls Gemini 1.5 Flash with a specific nutrition prompt.
3. Parses the returned JSON/Text for macros.
  </action>
</task>

<task id="05-03-T2" title="Add fallback arbitration logic">
  <wave>1</wave>
  <action>
Implement a method to decide if the LLM output is "closer to reality" or if it should merely trigger a warning.
  </action>
</task>

## Verification

- [ ] Mocked LLM responses are successfully parsed.
- [ ] Integration test (if API key available) confirms visual question answering works.

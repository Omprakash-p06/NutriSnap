---
phase: 03-sam2-glpn-vit-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/nutrisnap/pipeline/segmenter.py, src/nutrisnap/pipeline/depth.py, configs/pipeline/depth.yaml]
autonomous: true
requirements: [PREP-04, SEGM-02, MODL-02]
must_haves:
  truths:
    - "FoodSegmenterSAM2 can generate binary masks from images using SAM 2"
    - "DepthEstimatorGLPN can estimate depth maps using Hugging Face GLPN"
    - "Both models run on CUDA by default if available"
  artifacts:
    - path: "src/nutrisnap/pipeline/segmenter.py"
      provides: "FoodSegmenterSAM2 class"
    - path: "src/nutrisnap/pipeline/depth.py"
      provides: "DepthEstimatorGLPN class"
    - path: "configs/pipeline/depth.yaml"
      provides: "GLPN model configuration"
  key_links:
    - from: "src/nutrisnap/pipeline/segmenter.py"
      to: "transformers.Sam2Model"
      via: "SAM 2 model loading"
    - from: "src/nutrisnap/pipeline/depth.py"
      to: "transformers.GLPNForDepthEstimation"
      via: "GLPN model loading"
---

<objective>
Implement the first two stages of the 3-stage accuracy pipeline: SAM 2 segmentation and GLPN depth estimation.
These model adapters will be used in the updated preprocessing flow to generate 3D-aware features for the ViT regressor.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@misc/strategy_final_2026-04-16.md
@src/nutrisnap/pipeline/segmenter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement FoodSegmenterSAM2</name>
  <files>src/nutrisnap/pipeline/segmenter.py</files>
  <action>
    Add a new `FoodSegmenterSAM2` class to `src/nutrisnap/pipeline/segmenter.py`.
    - Use `transformers.Sam2Model` and `transformers.AutoProcessor` to load SAM 2 (specifically "facebook/sam2-hiera-tiny" or "facebook/sam2-hiera-small" to respect 4GB VRAM).
    - Implement a `segment(image)` method that returns a combined binary mask of food items.
    - Ensure CUDA is used if available.
    - Maintain compatibility with the existing `FoodSegmenter` interface where possible.
  </action>
  <verify>
    <automated>python -c "from nutrisnap.pipeline.segmenter import FoodSegmenterSAM2; s = FoodSegmenterSAM2(); print('SAM2 Loaded')"</automated>
  </verify>
  <done>FoodSegmenterSAM2 implemented and can be instantiated without error.</done>
</task>

<task type="auto">
  <name>Task 2: Implement DepthEstimatorGLPN</name>
  <files>src/nutrisnap/pipeline/depth.py, configs/pipeline/depth.yaml</files>
  <action>
    1. Create `configs/pipeline/depth.yaml` with GLPN model parameters (model_id: "vinvino02/glpn-nyu", device: "auto").
    2. Create `src/nutrisnap/pipeline/depth.py`.
    3. Implement `DepthEstimatorGLPN` using `transformers.GLPNForDepthEstimation` and `transformers.GLPNImageProcessor`.
    4. Implement an `estimate(image)` method that returns a normalized depth map (float32, 0-1).
    5. Ensure CUDA is used if available.
  </action>
  <verify>
    <automated>python -c "from nutrisnap.pipeline.depth import DepthEstimatorGLPN; d = DepthEstimatorGLPN(); print('GLPN Loaded')"</automated>
  </verify>
  <done>DepthEstimatorGLPN implemented and can be instantiated without error.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Model Adapters on Sample Image</name>
  <files>tests/test_adapters_sam2_glpn.py</files>
  <action>
    Create a verification script `tests/test_adapters_sam2_glpn.py` that:
    1. Loads a sample image (e.g., from `data/raw/archive (4)/imagery/realsense_overhead/dish_1550796634/rgb.png`).
    2. Runs `FoodSegmenterSAM2.segment()` and asserts a mask is returned.
    3. Runs `DepthEstimatorGLPN.estimate()` and asserts a depth map is returned.
    4. Prints VRAM usage to ensure it stays under 4GB.
  </action>
  <verify>
    <automated>pytest tests/test_adapters_sam2_glpn.py</automated>
  </verify>
  <done>Both models verified working on a real image within VRAM limits.</done>
</task>

</tasks>

<verification>
Check that both classes are implemented and can process a sample image.
Verify CUDA usage via `torch.cuda.is_available()` check inside classes.
</verification>

<success_criteria>
- `FoodSegmenterSAM2` implemented and verified.
- `DepthEstimatorGLPN` implemented and verified.
- VRAM usage < 4GB for both models loaded.
</success_criteria>

<output>
After completion, create `.planning/phases/03-sam2-glpn-vit-pipeline/03-01-SUMMARY.md`
</output>

# NutriSnap data configuration
raw_dir: "datasets/raw/archive (4)"
interim_dir: "datasets/interim"
processed_dir: "datasets/processed"
features_dir: "datasets/processed/features"
splits_dir: "datasets/splits"
external_dir: "datasets/external"
imagery_subdir: "imagery/realsense_overhead"
mvp_dish_count: 20
val_fraction: 0.15
test_fraction: 0.15
n_cv_folds: 5
random_seed: 42
calorie_bins: [0, 200, 400, 600, 800, 1000, 2000]
data:
  raw_path: "datasets/raw/archive (4)"
  imagery_subdir: "imagery/realsense_overhead"
